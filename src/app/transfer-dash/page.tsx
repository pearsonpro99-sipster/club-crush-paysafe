import TransferDashGame from './TransferDashGame';

export const metadata = {
  title: 'Transfer Deadline Dash | Sky Sports',
  description: 'A timed football connections puzzle. Group the clues before the window closes.',
};

export default function TransferDashPage() {
  return <TransferDashGame />;
}
